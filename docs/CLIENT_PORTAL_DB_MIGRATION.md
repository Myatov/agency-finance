# Безопасное изменение базы данных: добавление личного кабинета клиента

Инструкция по безопасному применению изменений схемы БД для личного кабинета клиента через SSH.

---

## ⚠️ ВАЖНО: Перед началом

1. **Сделайте полный бэкап БД** (см. [BACKUP_SERVER_DB.md](./BACKUP_SERVER_DB.md))
2. **Убедитесь, что у вас есть доступ к серверу по SSH**
3. **Выполняйте команды последовательно, не пропуская шаги**

---

## Шаг 1: Подключение к серверу и подготовка

```bash
# Подключитесь к серверу
ssh ПОЛЬЗОВАТЕЛЬ@IP_ИЛИ_ДОМЕН_СЕРВЕРА

# Перейдите в папку проекта
cd /var/www/agency-finance

# Проверьте, что вы в правильной директории
pwd
# Должно быть: /var/www/agency-finance
```

---

## Шаг 2: Создание бэкапа БД (ОБЯЗАТЕЛЬНО)

**Не пропускайте этот шаг!** Бэкап нужен для отката в случае проблем.

```bash
# Узнайте параметры подключения к БД
grep DATABASE_URL .env

# Извлеките из строки DATABASE_URL:
# - ЛОГИН (между :// и :)
# - ПАРОЛЬ (между : и @)
# - ИМЯ_БД (между последним / и ?)

# Создайте бэкап (подставьте свои значения)
export PGPASSWORD='ПАРОЛЬ_ИЗ_ENV'
pg_dump -h localhost -U ЛОГИН -d ИМЯ_БД -F c -f /tmp/backup_before_portal_$(date +%Y%m%d_%H%M%S).dump
unset PGPASSWORD

# Проверьте, что файл создан
ls -lh /tmp/backup_before_portal_*.dump

# Скачайте бэкап на локальный компьютер (выполните на своём компьютере, не на сервере)
# scp ПОЛЬЗОВАТЕЛЬ@IP_СЕРВЕРА:/tmp/backup_before_portal_*.dump ~/Downloads/
```

**Пример:**
```bash
export PGPASSWORD='SecretPass123'
pg_dump -h localhost -U agency_finance -d agency_finance -F c -f /tmp/backup_before_portal_$(date +%Y%m%d_%H%M%S).dump
unset PGPASSWORD
ls -lh /tmp/backup_before_portal_*.dump
```

---

## Шаг 3: Обновление кода проекта

```bash
# Убедитесь, что вы в папке проекта
cd /var/www/agency-finance

# Получите последние изменения из Git
git pull origin main

# Проверьте, что файл миграции на месте
ls -la prisma/add-client-portal-access.sql

# Если файла нет, проверьте, что git pull выполнился успешно
git status
```

---

## Шаг 4: Применение миграции БД

**Вариант A: Применение SQL-миграции напрямую (рекомендуется)**

Этот способ безопаснее, чем `prisma db push`, так как не затрагивает другие таблицы.

```bash
# Убедитесь, что вы в папке проекта
cd /var/www/agency-finance

# Примените SQL-миграцию (подставьте свои данные из .env)
export PGPASSWORD='ПАРОЛЬ_ИЗ_ENV'
psql -h localhost -U ЛОГИН -d ИМЯ_БД -f prisma/add-client-portal-access.sql
unset PGPASSWORD
```

**Пример:**
```bash
export PGPASSWORD='SecretPass123'
psql -h localhost -U agency_finance -d agency_finance -f prisma/add-client-portal-access.sql
unset PGPASSWORD
```

**Что делает эта миграция:**
- Создаёт таблицу `ClientPortalAccess` (если её ещё нет)
- Добавляет индексы и внешние ключи
- Не изменяет существующие таблицы

---

## Шаг 5: Проверка успешности миграции

```bash
# Подключитесь к БД и проверьте наличие таблицы
export PGPASSWORD='ПАРОЛЬ_ИЗ_ENV'
psql -h localhost -U ЛОГИН -d ИМЯ_БД -c "\dt ClientPortalAccess"
unset PGPASSWORD

# Должна вывестись информация о таблице ClientPortalAccess
# Если таблицы нет — миграция не применилась, проверьте ошибки выше
```

**Проверка структуры таблицы:**
```bash
export PGPASSWORD='ПАРОЛЬ_ИЗ_ENV'
psql -h localhost -U ЛОГИН -d ИМЯ_БД -c "\d ClientPortalAccess"
unset PGPASSWORD
```

Должны быть колонки: `id`, `clientId`, `passwordHash`, `accessToken`, `createdByUserId`, `createdAt`, `updatedAt`.

---

## Шаг 6: Обновление Prisma Client

После изменения схемы БД нужно перегенерировать Prisma Client:

```bash
cd /var/www/agency-finance

# Перегенерируйте Prisma Client
npm run db:generate

# Проверьте, что команда выполнилась без ошибок
echo $?
# Должно быть 0 (успех)
```

---

## Шаг 7: Перезапуск приложения

```bash
# Перезапустите приложение через PM2
pm2 restart agency-finance --update-env

# Проверьте статус
pm2 status

# Проверьте логи (должны быть без ошибок)
pm2 logs agency-finance --lines 50
```

**Если есть ошибки:**
- Проверьте логи: `pm2 logs agency-finance`
- Убедитесь, что Prisma Client перегенерирован: `npm run db:generate`
- Проверьте, что таблица создана: см. Шаг 5

---

## Шаг 8: Проверка работоспособности

1. **Откройте сайт в браузере** и войдите в CRM
2. **Перейдите в раздел "Клиенты"**
3. **Откройте любого клиента** и нажмите кнопку **"Личный кабинет"**
4. **Попробуйте создать доступ** (введите пароль и нажмите "Создать доступ")
5. **Скопируйте ссылку** и откройте её в браузере (в режиме инкогнито)
6. **Введите пароль** — должен произойти вход в личный кабинет

Если всё работает — миграция успешна! ✅

---

## 🔄 Откат изменений (если что-то пошло не так)

Если после миграции возникли проблемы, можно откатить изменения:

```bash
# 1. Остановите приложение
pm2 stop agency-finance

# 2. Восстановите БД из бэкапа (подставьте свои данные)
export PGPASSWORD='ПАРОЛЬ_ИЗ_ENV'
pg_restore -h localhost -U ЛОГИН -d ИМЯ_БД --clean --if-exists /tmp/backup_before_portal_YYYYMMDD_HHMMSS.dump
unset PGPASSWORD

# 3. Удалите таблицу ClientPortalAccess (если нужно)
export PGPASSWORD='ПАРОЛЬ_ИЗ_ENV'
psql -h localhost -U ЛОГИН -d ИМЯ_БД -c "DROP TABLE IF EXISTS \"ClientPortalAccess\" CASCADE;"
unset PGPASSWORD

# 4. Откатите код (если нужно)
cd /var/www/agency-finance
git reset --hard HEAD~1  # Осторожно! Это удалит последний коммит

# 5. Перегенерируйте Prisma Client
npm run db:generate

# 6. Перезапустите приложение
pm2 restart agency-finance --update-env
```

---

## Альтернативный способ: через Prisma db push

Если SQL-миграция не подходит, можно использовать `prisma db push`, но **будьте осторожны** — он может предупредить о возможной потере данных:

```bash
cd /var/www/agency-finance

# Обновите код
git pull origin main

# Примените схему (с подтверждением потери данных, если потребуется)
npx prisma db push --accept-data-loss

# Перегенерируйте Prisma Client
npm run db:generate

# Перезапустите приложение
pm2 restart agency-finance --update-env
```

⚠️ **Внимание:** `--accept-data-loss` может удалить данные, если есть конфликты схемы. Используйте только если уверены, что это безопасно.

---

## Чеклист безопасности

- [ ] Создан полный бэкап БД перед изменениями
- [ ] Бэкап скачан на локальный компьютер
- [ ] Код обновлён через `git pull`
- [ ] Миграция применена через `psql` или `prisma db push`
- [ ] Таблица `ClientPortalAccess` создана и проверена
- [ ] Prisma Client перегенерирован (`npm run db:generate`)
- [ ] Приложение перезапущено (`pm2 restart`)
- [ ] Функциональность проверена в браузере
- [ ] Логи приложения не содержат ошибок

---

## Возможные проблемы и решения

### Проблема: "relation ClientPortalAccess already exists"

**Решение:** Таблица уже существует. Это нормально, если миграция запускалась ранее. Продолжайте с шага 6.

### Проблема: "permission denied" при выполнении psql

**Решение:** Убедитесь, что используете правильного пользователя БД из `.env`. Проверьте права:
```bash
sudo -u postgres psql -c "\du"
```

### Проблема: Приложение не запускается после миграции

**Решение:**
1. Проверьте логи: `pm2 logs agency-finance`
2. Убедитесь, что Prisma Client перегенерирован: `npm run db:generate`
3. Проверьте, что таблица существует: см. Шаг 5
4. Перезапустите: `pm2 restart agency-finance --update-env`

### Проблема: Ошибка "Database schema error: ClientPortalAccess model not available"

**Решение:** Prisma Client не был перегенерирован. Выполните:
```bash
npm run db:generate
pm2 restart agency-finance --update-env
```

---

## Дополнительная информация

- **Бэкапы БД:** см. [BACKUP_SERVER_DB.md](./BACKUP_SERVER_DB.md)
- **Синхронизация БД:** см. [DATABASE_SYNC.md](./DATABASE_SYNC.md)
- **Команды сервера:** см. [SERVER_COMMANDS.md](./SERVER_COMMANDS.md)

---

**Дата создания:** 2026-02-08  
**Версия:** 1.0
