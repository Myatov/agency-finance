name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /var/www/agency-finance
            git pull origin main
            echo "🔄 Installing dependencies..."
            npm ci || npm install
            echo "🔄 Generating Prisma Client..."
            npm run db:generate
            echo "🔄 Applying DB schema changes..."
            npx prisma db push --skip-generate || echo "⚠️ DB push failed, continuing..."
            echo "🔄 Applying DB migration (Expense.legalEntityId) if needed..."
            npx prisma db execute --file prisma/add-expense-legal-entity.sql || true
            echo "🔄 Applying DB migration (User extra fields) if needed..."
            npx prisma db execute --file prisma/add-user-fields.sql || true
            echo "🔄 Applying DB migration (Contracts & Closeout tables) if needed..."
            npx prisma db execute --file prisma/add-contracts-closeout-tables.sql || true
            echo "🔄 Applying DB migration (Client requisites columns) if needed..."
            npx prisma db execute --file prisma/add-client-requisites-columns.sql || true
            echo "🔄 Applying DB migration (Niche table) if needed..."
            npx prisma db execute --file prisma/create-niche-table.sql || true
            echo "🔄 Stopping old processes..."
            pm2 stop agency-finance 2>/dev/null || true
            pkill -f "next-server" 2>/dev/null || true
            sleep 2
            echo "🔄 Cleaning old build..."
            rm -rf .next
            echo "🔄 Building application..."
            npm run build
            echo "✅ Build completed"
            echo "🔄 Restarting application..."
            pm2 restart agency-finance --update-env || pm2 start npm --name agency-finance -- start
            echo "✅ Application restarted"
            sleep 15
            echo "📋 PM2 Status:"
            pm2 status
            echo ""
            echo "📋 Checking application logs (last 30 lines):"
            pm2 logs agency-finance --lines 30 --nostream || echo "⚠️ Could not get logs"
            echo ""
            echo "📋 Checking for errors:"
            pm2 logs agency-finance --err --lines 20 --nostream || echo "⚠️ No errors found"
            echo ""
            echo "📋 Testing application health:"
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 2>&1 || echo "000")
            echo "HTTP Status: $HTTP_CODE"
            if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "302" ] && [ "$HTTP_CODE" != "401" ] && [ "$HTTP_CODE" != "404" ]; then
              echo "⚠️ Application not responding (HTTP $HTTP_CODE), attempting quick fix..."
              echo "🔄 Stopping all processes..."
              pm2 stop agency-finance 2>/dev/null || true
              pm2 delete agency-finance 2>/dev/null || true
              pkill -f "next-server" 2>/dev/null || true
              sleep 3
              echo "🔄 Starting application..."
              pm2 start npm --name agency-finance -- start || pm2 restart agency-finance --update-env
              sleep 10
              echo "📋 Final health check:"
              HTTP_CODE2=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 2>&1 || echo "000")
              echo "HTTP Status after recovery: $HTTP_CODE2"
              if [ "$HTTP_CODE2" != "200" ] && [ "$HTTP_CODE2" != "302" ] && [ "$HTTP_CODE2" != "401" ] && [ "$HTTP_CODE2" != "404" ]; then
                echo "⚠️ Application still not responding. Check logs: pm2 logs agency-finance --lines 100"
              fi
            fi
