#!/bin/bash
# Скрипт для исправления Prisma Client на сервере
# Использование: bash scripts/fix-prisma-client.sh

set -e

echo "🔍 Проверка Prisma Client..."

# Переходим в директорию проекта (если скрипт запущен не из корня)
cd "$(dirname "$0")/.." || exit 1

echo "📦 Генерация Prisma Client..."
npm run db:generate

echo "✅ Prisma Client перегенерирован"
echo ""
echo "⚠️  ВАЖНО: Теперь необходимо перезапустить приложение!"
echo ""
echo "Для перезапуска используйте одну из команд:"
echo "  - pm2 restart app"
echo "  - systemctl restart your-app"
echo "  - или перезапустите процесс Node.js другим способом"
