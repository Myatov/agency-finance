# Скопируйте этот файл в .env и подставьте свои значения:
#   cp .env.example .env
# Затем отредактируйте .env (не коммитьте его в репозиторий).

# --- База данных ---
# Строка подключения к PostgreSQL (обязательно для Prisma).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"

# --- Приложение (URL для ссылок и PDF) ---
# Публичный адрес приложения без завершающего слеша.
# Нужен для корректных ссылок в PDF (QR-код), письмах и личном кабинете.
# Локально: http://localhost:3000
# На сервере: https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Каталог для сохранения сгенерированных PDF счетов ---
# Абсолютный или относительный путь. Если не задан — используется ./invoices-pdf в корне проекта.
# INVOICE_PDF_DIR="/var/app/invoices-pdf"

# --- Авторизация (куки) ---
# В продакшене за HTTPS установите true, чтобы куки ставились с флагом Secure.
# SECURE_COOKIES="true"

# --- Telegram (опционально) ---
# Для уведомлений о расходах и тестового эндпоинта.
# TELEGRAM_BOT_TOKEN=""
# TELEGRAM_EXPENSES_CHAT_ID=""

# --- Загрузки файлов ---
# Каталог для загружаемых файлов. По умолчанию — ./uploads в корне проекта.
# UPLOAD_DIR="/var/app/uploads"
